<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dayi.demo.student.dao.StudentDao">

    <resultMap id="studentResultMap" type="Student">
        <id property="id" column="id" />
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
    </resultMap>

    <sql id="selectAllColunm">
        SELECT id,name,age,create_time,update_time FROM
    </sql>

    <!-- id 查询 -->
    <select id="get" resultMap="studentResultMap" >
        <include refid="selectAllColunm"/>
        student t WHERE t.id = #{id}
    </select>

    <!-- 查询全部 -->
    <select id="findAll" resultMap="studentResultMap" >
        <include refid="selectAllColunm"/> student t
    </select>

    <insert id="add" useGeneratedKeys="true" keyProperty="id">
        insert into student (name,age,create_time,update_time)
        values (#{name},#{age},#{createTime},#{updateTime})
    </insert>

    <update id="update">
        update student
        <set>
            <if test="name != null">name=#{name},</if>
            <if test="age != null">age=#{age},</if>
            <if test="updateTime != null">update_time=#{updateTime}</if>
        </set>
        where id=#{id}
    </update>






</mapper>